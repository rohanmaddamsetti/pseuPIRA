[project]
name = "pseupira"
version = "0.1.0"
description = "rapid plasmid copy number inference"
readme = "README.md"
requires-python = ">=3.12.9"
dependencies = [
    "biopython>=1.85",
    "htseq>=2.0.9",
    "numpy>=2.2.3",
    "polars==1.24.0",
]
